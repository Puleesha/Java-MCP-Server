global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "mcp_java_benchmark"
    static_configs:
      - targets: ["host.docker.internal:9100"]
        labels:
          variant: "baseline"
      - targets: ["host.docker.internal:9101"]
        labels:
          variant: "structured"